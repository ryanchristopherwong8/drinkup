<% content_for :header do %>
  <%= javascript_include_tag "conversations" %>   
<% end %>
<div class="container-fluid">
  <h1 id="page_title">Profile</h1>
  <div class="row">
    <div class="col-md-6">
        <%= image_tag @user.avatar.url(:medium), class: "img-responsive img-rounded" %>
        <p><strong>First Name:</strong><%=@user.first_name %></p>
        <p><strong>Last Name:</strong><%=@user.last_name %></p>
        <p><strong>Email:</strong><%= @user.email %></p>
        <p><strong>Date of birth:</strong><%= @user.date_of_birth %></p>
    </div>

    <div class="col-md-6">
        <p><strong>Conversations: <span class="glyphicon glyphicon-info-sign" aria-hidden="true" data-toggle="tooltip" data-placement="bottom" title="What Conversation topics do you enjoy chatting about? Share your favorite Conversations with others by adding them below. "></span></strong></p>
        <script>
          $(function () {
            $('[data-toggle="tooltip"]').tooltip()
            })
        </script>
        <div id="user_conversations" class="clearfix">
        <% @user.conversations.each do |conversation| %>
          <span class="conversation_tag"><%= "#{conversation.name}" %></span>
        <% end %>
        </div>
        <%= link_to 'Edit Conversations', "#", id: "conversation_link" if @user == current_user  %>
        <div id="conversation_select" class=" col-md-12"></div>
    </div>

  </div>
<%= link_to 'Edit', edit_user_path(@user), class: "btn btn-default" if @user == current_user %>
<%= link_to 'Destroy', user_path(@user),
              method: :delete,
              data: { confirm: 'Are you sure you want to delete your profile?' }, class: "btn btn-default" if @user == current_user %>
</div>