<!-- left column -->
<div class="col-md-3">
	<div class="text-center">
    <%= image_tag @user.avatar.url(:medium), class: "avatar img-circle", alt: "avatar" %>
    	<% if params[:action] == "edit" %>
    		<h6>Upload a different photo...</h6>
        	<%= curr_user.file_field :avatar, class: "form-control" %>
    	<%end%>
	</div>
</div>